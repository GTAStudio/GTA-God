# GTAGod 朋友部署指南

> 这是一份简化的部署指南，帮助朋友快速部署 GTAGod 代理服务。

## 前置准备

在开始之前，你需要准备：

### 1. 云服务器

- **推荐配置**: 1核 512MB 内存即可
- **系统**: Ubuntu 22.04 / Debian 12 (推荐)
- **位置**: 香港、新加坡、日本、美国等
- **推荐商家**: 
  - [Vultr](https://www.vultr.com/) - $5/月起
  - [DigitalOcean](https://www.digitalocean.com/) - $4/月起
  - [Linode](https://www.linode.com/) - $5/月起
  - [搬瓦工](https://bandwagonhost.com/) - 适合新手

### 2. 域名

- 任意域名即可，推荐 `.com` 或 `.net`
- 购买后将 DNS 托管到 Cloudflare

### 3. Cloudflare 账号

1. 注册 [Cloudflare](https://cloudflare.com/)
2. 添加你的域名到 Cloudflare
3. 获取 API Token:
   - 登录 Cloudflare Dashboard
   - 右上角头像 → My Profile → API Tokens
   - Create Token → Edit zone DNS 模板
   - Zone Resources: 选择你的域名
   - 创建并保存 Token

---

## 快速部署 (3 分钟)

### 步骤 1: 连接服务器

使用 SSH 连接到你的服务器：

```bash
ssh root@你的服务器IP
```

### 步骤 2: 下载脚本

```bash
# 创建目录
mkdir -p /root/gtagod && cd /root/gtagod

# 下载脚本包 (让 Jason 发给你)
# 或者从私有仓库下载
```

### 步骤 3: 运行部署脚本

```bash
chmod +x run.sh
./run.sh
```

### 步骤 4: 按提示输入

脚本会自动进入交互模式，提示你输入：

1. **部署模式** - 推荐选择 1 (NaiveProxy Only)
2. **域名** - 你的域名，例如 `example.com`
3. **Cloudflare API Token** - 从 Cloudflare 获取
4. **用户名** - 自定义，例如 `myuser`
5. **密码** - 自定义强密码

> 💡 如果目录下有 `gtagod.conf` 配置文件，脚本会自动加载。
> 使用 `./run.sh -i` 可强制进入交互模式。

### 步骤 5: 等待完成

脚本会自动：
- 安装 Docker
- 配置网络优化 (BBR)
- 申请 SSL 证书
- 启动代理服务

---

## 部署模式说明

| 模式 | 复杂度 | 说明 |
|------|--------|-----|
| 1) NaiveProxy Only | ⭐ | 最简单，只需域名 |
| 2) NaiveProxy + AnyTLS | ⭐⭐ | 更隐蔽的 TLS 隧道 |
| 3) NaiveProxy + AnyReality | ⭐⭐⭐ | 无需证书，伪装微软 |
| 4) 全部启用 | ⭐⭐⭐⭐ | 全功能，443 共享 |

**新手推荐**: 选择模式 1，简单稳定！

---

## 客户端配置

### NaiveProxy (模式 1)

部署完成后，你会看到类似信息：

```
域名: example.com
用户: myuser
密码: (你输入的密码)
```

#### Windows / macOS

1. 下载 [NaiveProxy](https://github.com/klzgrad/naiveproxy/releases)
2. 创建 `config.json`:

```json
{
  "listen": "socks://127.0.0.1:1080",
  "proxy": "https://myuser:你的密码@proxy.example.com"
}
```

3. 运行: `./naive config.json`

#### Android

1. 下载 [NekoBox](https://github.com/MatsuriDayo/NekoBoxForAndroid/releases)
2. 添加 NaiveProxy 节点
3. 填入服务器信息

#### iOS

1. 下载 Shadowrocket (App Store)
2. 添加 Naive 类型节点
3. 填入服务器信息

---

## 安全组/防火墙设置

确保在云服务商控制台开放以下端口：

| 端口 | 协议 | 用途 |
|------|------|-----|
| 22 | TCP | SSH 管理 |
| 80 | TCP | HTTP (证书申请) |
| 443 | TCP | HTTPS (所有代理服务) |

> **说明**: 所有服务（NaiveProxy、AnyTLS、AnyReality）都通过 443 端口提供，Caddy L4 根据 SNI 自动分流。
> 8443/8444 是容器内部端口，无需对外开放。

---

## 常见问题

### Q: 部署失败怎么办？

1. 检查域名是否正确解析到服务器IP
2. 检查 Cloudflare Token 权限是否正确
3. 查看日志: `docker logs caddy`

### Q: 连接不上怎么办？

1. 检查安全组/防火墙端口是否开放
2. 检查客户端配置是否正确
3. 尝试重启服务: `docker restart caddy`

### Q: 证书申请失败？

Caddy 会自动切换 CA 重试：
- Let's Encrypt → ZeroSSL

如果都失败，检查:
1. 域名 DNS 是否正确
2. Cloudflare Token 权限
3. 等待几分钟后重试

### Q: 如何更新服务？

```bash
docker pull aizhihuxiao/gtagod:latest
docker restart caddy
```

### Q: 如何查看连接日志？

```bash
# Caddy 日志
docker logs -f caddy

# 如果启用了 sing-box
docker exec caddy tail -f /var/log/sing-box/sing-box.log
```

---

## 管理命令速查

```bash
# 查看状态
docker ps

# 查看日志
docker logs -f caddy

# 重启服务
docker restart caddy

# 停止服务
docker stop caddy

# 启动服务
docker start caddy

# 重新部署 (会覆盖配置)
./run.sh
```

---

## 需要帮助？

如果遇到问题，请联系 Jason 并提供：

1. 错误截图
2. 运行 `docker logs caddy` 的输出
3. 服务器系统版本

---

> 🔒 **安全提示**: 
> - 不要泄露你的密码和 API Token
> - 定期更新 Docker 镜像
> - 建议 SSH 使用密钥登录
